{% import '@SyliusAdmin/Macro/breadcrumb.html.twig' as breadcrumb %}

{% set breadcrumbs = [
    { label: 'sylius.ui.administration'|trans, url: path('sylius_admin_dashboard') },
    { label: 'behappy_invoice_plugin.ui.menu.invoices'|trans, url: path('behappy_invoice_plugin_admin_invoice_index') },
    { label: 'behappy_invoice_plugin.ui.show_invoice'|trans({ '%invoice_number%': invoice.number }) }
]
%}

{{ breadcrumb.crumble(breadcrumbs) }}
